name: hackathonapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  image_picker: ^1.0.7
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 👇 Add your asset images here (make sure the folder & files exist)
  assets:
    - assets/pneumonia.jpg
    - assets/brain_tumor.jpg
    - assets/lung_cancer.jpg
    - assets/chatbot.jpg